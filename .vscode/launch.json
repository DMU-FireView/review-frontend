{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Flutter (Web)",
      "request": "launch",
      "type": "dart",
      "args": [
        "-d", "chrome",
        "--dart-define=API_BASE_URL=http://localhost:8080"
      ],
      "preLaunchTask": "Start Local Proxy"
    }
  ]
}
